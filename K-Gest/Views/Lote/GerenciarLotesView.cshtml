@using System.Data
@model DataTable

@{
    ViewData["Title"] = "Gerenciar Lotes";
    var unidadeMed = ViewBag.UnidadeMed?.ToString() ?? "UN";
}

<div class="container-fluid px-4 py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div class="d-flex align-items-center gap-3">
            <div class="icon-shape-salmon">
                <i class="bi bi-layers-fill"></i>
            </div>
            <div>
                <span class= th-col-label"text-secondary text-uppercase fw-bold small tracking-wider">Lotes Cadastrados do Insumo</span>
                <h2 class="fw-bold text-dark m-0 page-title">
                    @ViewBag.NomeInsumo <span class="fs-5 text-muted fw-normal">(@unidadeMed)</span>
                </h2>
            </div>
        </div>
        <div class="d-flex gap-2">
            <a href="/Lote/InserirExibir?idInsumo=@ViewBag.IdInsumo" class="btn-salmon-action d-flex align-items-center gap-2 shadow-sm">
                <i class="bi bi-plus-lg"></i> Cadastrar Novo Lote
            </a>
            <a href="/Lote/Selecionar" class="btn btn-outline-secondary d-flex align-items-center gap-2 btn-back-outline">
                <i class="bi bi-arrow-left"></i> Voltar
            </a>
        </div>
    </div>

    @if (TempData["MsgSucesso"] != null)
    {
        <div class="alert alert-success border-0 shadow-sm d-flex align-items-center mb-4 alert-success-feedback" role="alert">
            <i class="bi bi-check-circle-fill me-2 fs-5"></i>
            <div class="fw-medium small">@TempData["MsgSucesso"]</div>
        </div>
    }

    <div class="card border-0 shadow-premium card-section">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0" style="min-width: 800px;">
                <thead class="table-head-default">
                    <tr>
                        <th class= th-col-label"ps-4 py-3 text-secondary text-uppercase fw-bold" style="width: 120px;">Nº do Lote</th>
                        <th class= th-col-label"py-3 text-secondary text-uppercase fw-bold">Data de Fabricação</th>
                        <th class= th-col-label"py-3 text-secondary text-uppercase fw-bold">Data de Validade</th>
                        <th class= th-col-label"py-3 text-secondary text-uppercase fw-bold text-end">Quantidade</th>
                        <th class= th-col-label"py-3 text-secondary text-uppercase fw-bold text-center" style="width: 220px;">Status Validade</th>
                        <th class= th-col-label"pe-4 py-3 text-secondary text-uppercase fw-bold text-center" style="width: 150px;">Ações</th>
                    </tr>
                </thead>
                <tbody class="border-0">
                    @if (Model != null && Model.Rows.Count > 0)
                    {
                        foreach (DataRow dr in Model.Rows)
                        {
                            DateTime dtValidade = Convert.ToDateTime(dr["dtValidade"]);
                            string situacao = dr["situacao"]?.ToString() ?? "Regular / Próprio para consumo";
                            int diasRestantes = (dtValidade.Date - DateTime.Now.Date).Days;
                            string mensagemDias = diasRestantes == 0 ? "Hoje"
                                                                     : diasRestantes == 1 ? "Amanhã"
                                                                     : $"em {diasRestantes} Dias";
                            <tr>
                                <td class="ps-4 py-3">
                                    <span class="fw-bold text-dark font-monospace fs-6">#@dr["numLote"]</span>
                                </td>

                                <td class="py-3 text-secondary">
                                    @Convert.ToDateTime(dr["dtFabricacao"]).ToString("dd/MM/yyyy")
                                </td>

                                <td class="py-3 text-dark fw-medium">
                                    @dtValidade.ToString("dd/MM/yyyy")
                                </td>

                                <td class="text-end py-3 fw-bold text-dark">
                                    @(unidadeMed == "UN"
                                        ? Convert.ToDecimal(dr["quantidade"]).ToString("N0")
                                        : Convert.ToDecimal(dr["quantidade"]).ToString("N2"))
                                    <span class="text-muted fw-normal small">@unidadeMed</span>
                                </td>

                                <td class="text-center py-3">
                                    @if (situacao == "Vencido")
                                    {
                                        <span class="badge bg-danger-subtle text-danger px-2.5 py-1.5 d-inline-flex align-items-center gap-1" style="border-radius: 30px; font-size: 11px; font-weight: 600;">
                                            <i class="bi bi-x-circle-fill"></i> Vencido
                                        </span>
                                    }
                                    else if (situacao == "Alerta")
                                    {

                                        <span class="badge bg-warning-subtle text-warning px-2.5 py-1.5 d-inline-flex align-items-center gap-1 badge-pill badge-pill-alerta">
                                            <i class="bi bi-exclamation-triangle-fill"></i> Alerta (Vence @mensagemDias)
                                        </span>
                                    }
                                    else if (situacao == "Próximo da data de vencimento")
                                    {
                                        <span class="badge bg-info-subtle text-info px-2.5 py-1.5 d-inline-flex align-items-center gap-1 badge-pill badge-pill-proximo">
                                            <i class="bi bi-clock-history"></i> Próximo do Vencimento
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-success-subtle text-success px-2.5 py-1.5 d-inline-flex align-items-center gap-1 badge-pill badge-pill-entrada">
                                            <i class="bi bi-check-circle-fill"></i> Próprio para Consumo
                                        </span>
                                    }
                                </td>

                                <td class="pe-4 text-center py-3">
                                    <div class="d-flex justify-content-center gap-2">
                                        <a href="/Lote/AlterarExibir/@dr["idLote"]"
                                           class="btn-action-modern btn-edit-salmon"
                                           title="Editar Lote">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>

                                        <a href="/Lote/ExcluirExibir/@dr["idLote"]"
                                           class="btn-action-modern btn-delete-salmon"
                                           title="Excluir Lote">
                                            <i class="bi bi-trash3"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center text-muted py-5">
                                <i class="bi bi-info-circle fs-3 d-block mb-2 text-secondary"></i>
                                <span class="fw-medium">Nenhum lote ativo encontrado para este insumo.</span>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>