@using System.Data
@model DataTable

@{
    ViewData["Title"] = "Controle de Lotes";
}

<div class="container-fluid px-4 py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div class="d-flex align-items-center gap-3">
            <div class="icon-shape-salmon">
                <i class="bi bi-layers-half"></i>
            </div>
            <div>
                <h2 class="fw-bold text-dark m-0" style="font-size: 24px; letter-spacing: -0.5px;">Controle de Estoque por Lotes</h2>
                <p class="text-muted small m-0 mt-1">Selecione um insumo para visualizar, rastrear ou adicionar novos lotes de validade</p>
            </div>
        </div>
    </div>

    @if (TempData["MsgSucesso"] != null)
    {
        <div class="alert alert-success border-0 shadow-sm d-flex align-items-center mb-4" role="alert" style="background-color: #ecfdf5; color: #065f46; border-radius: 12px;">
            <i class="bi bi-check-circle-fill me-2 fs-5"></i>
            <div class="fw-medium small">@TempData["MsgSucesso"]</div>
        </div>
    }

    @if (TempData["MsgErro"] != null)
    {
        <div class="alert alert-danger border-0 shadow-sm d-flex align-items-center mb-4" role="alert" style="background-color: #fef2f2; color: #991b1b; border-radius: 12px;">
            <i class="bi bi-exclamation-triangle-fill me-2 fs-5"></i>
            <div class="fw-medium small">@TempData["MsgErro"]</div>
        </div>
    }

    @if (Model == null || Model.Rows.Count == 0)
    {
        <div class="card border-0 shadow-premium py-5 text-center" style="border-radius: 20px;">
            <div class="card-body d-flex flex-column align-items-center justify-content-center p-5">
                <div class="rounded-circle d-flex align-items-center justify-content-center mb-4"
                     style="background-color: #fff1ed; width: 80px; height: 80px; color: #ff5742;">
                    <i class="bi bi-box-seam" style="font-size: 36px;"></i>
                </div>
                <h4 class="fw-bold text-dark mb-2">Nenhum insumo cadastrado</h4>
                <p class="text-muted mx-auto mb-4" style="max-width: 440px; font-size: 15px;">
                    Para gerenciar ou criar lotes, você precisa ter pelo menos um insumo cadastrado no sistema.
                </p>
                <a href="/Insumos/InserirExibir" class="btn-salmon-action d-inline-flex align-items-center gap-2 px-4 py-2.5 shadow-sm">
                    <i class="bi bi-plus-circle"></i> Cadastrar meu Primeiro Insumo
                </a>
            </div>
        </div>
    }
    else
    {
        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-xl-4 g-4">
            @foreach (DataRow dr in Model.Rows)
            {
                // Identificação dinâmica de colunas para evitar quebras por CapsLock
                string colId = dr.Table.Columns.Contains("idInsumo") ? "idInsumo" : (dr.Table.Columns.Contains("IdInsumo") ? "IdInsumo" : dr.Table.Columns[0].ColumnName);
                string colNome = dr.Table.Columns.Contains("nomeInsumo") ? "nomeInsumo" : (dr.Table.Columns.Contains("NomeInsumo") ? "NomeInsumo" : "Nome");
                string colUnidade = dr.Table.Columns.Contains("unidadeMed") ? "unidadeMed" : (dr.Table.Columns.Contains("UnidadeMed") ? "UnidadeMed" : "unidadeMed");

                var idInsumo = dr[colId];
                var nomeInsumo = dr[colNome];
                var unidadeMed = dr[dr.Table.Columns.Contains(colUnidade) ? colUnidade : dr.Table.Columns[2].ColumnName]; // Fallback seguro para unidade

                <div class="col">
                    <div class="card h-100 border-0 shadow-premium lote-card" style="border-radius: 20px; transition: transform 0.2s ease, box-shadow 0.2s ease;">
                        <div class="card-body d-flex flex-column justify-content-between p-4">
                            <div>
                                <div class="d-flex justify-content-between align-items-start gap-2 mb-3">
                                    <h5 class="fw-bold text-dark mb-0 fs-6 lh-base text-truncate-2" title="@nomeInsumo">@nomeInsumo</h5>
                                    <span class="badge bg-light text-secondary border px-2 py-1.5 font-monospace" style="border-radius: 6px; font-size: 11px;">@unidadeMed</span>
                                </div>
                                <div class="d-flex align-items-center gap-1.5 text-secondary mb-2" style="font-size: 12px; font-weight: 500;">
                                    <span class="badge font-monospace" style="background-color: #f1f5f9; color: #64748b; border-radius: 6px; font-size: 11px;">#@idInsumo</span>
                                    <span class="text-muted small">Cód. Insumo</span>
                                </div>
                            </div>

                            <div class="mt-4">
                                <a href="/Lote/GerenciarLotes?id=@idInsumo" class="btn-action-block btn-edit-salmon d-flex align-items-center justify-content-center gap-2 w-100">
                                    <i class="bi bi-eye-fill"></i> Gerenciar Lotes
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>